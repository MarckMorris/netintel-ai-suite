[pytest]
testpaths = tests project1_anomaly_detection/tests
python_files = test_*.py

# --strict-markers turns a mistyped marker into an error rather than a test
# that silently never runs.
addopts =
    --strict-markers
    --tb=short

markers =
    slow: needs an optional heavy dependency (TensorFlow, PyTorch)
