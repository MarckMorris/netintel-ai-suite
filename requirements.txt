# Core dependencies. Enough to run the alert correlation engine, the SD-WAN
# environment and the telemetry generator, and to run the test suite.
#
# The heavy machine-learning stacks are split into separate files so a clone
# does not pull several gigabytes of wheels to look at a 200-line correlation
# engine:
#
#   requirements-anomaly.txt    TensorFlow, for the LSTM autoencoder
#   requirements-sdwan.txt      PyTorch + Stable-Baselines3, for the DQN agent
#   requirements-api.txt        FastAPI inference service
#   requirements-dashboard.txt  Streamlit dashboard
#   requirements-dev.txt        test and lint tooling
#
# Removed in the 2026 cleanup because nothing imported them:
# sentence-transformers, transformers, networkx, seaborn, matplotlib, redis.
# The correlation engine used to load a 90 MB sentence-transformer model on
# construction and never call it.

pandas>=2.1
numpy>=1.24,<3.0
scikit-learn>=1.3
gymnasium>=0.29
joblib>=1.3
